@{
//Set database connectie

//db = connectie

/*if (Request.QueryString["tabel"] !== null)
{
    string table = Request.QueryString["tabel"];

    var inhoud = db.Query(@"select naam, prijs FROM " + table + "");
    foreach (var row in inhoud)
    {
        response.write(row[naam] + "," + row[priks);
    }

)
*/

}